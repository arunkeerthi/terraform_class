# terraform_class
Note: Before execute the script should be create:
$ mkdir .aws
$ vi config
[dev]
region = us-east-1
output = json

$ vi credentials
[dev]
aws_access_key_id = ************
aws_secret_access_key = ******************
